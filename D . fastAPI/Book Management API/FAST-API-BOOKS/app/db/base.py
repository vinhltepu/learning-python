from sqlaichemy.orm import declarative_base

base = declarative_base() # Tạo một lớp cơ sở (base class) cho các mô hình (models) của SQLAlchemy. Lớp này sẽ được sử dụng để định nghĩa các bảng trong cơ sở dữ liệu và cung cấp các phương thức để tương tác với cơ sở dữ liệu thông qua các đối tượng mô hình.